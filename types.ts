export interface Article {
    headline: string
    subheadline: string
    body: string
    category: string
    author: string
    publish_date: string
    tags: string[]
    video_urls: string[]
    image_urls: string[]
  }